# IncEventGS 论文深度分析

> 基于代码逆向分析 + 论文理解。训练中实时验证。

## 一、一句话概括

**用事件相机（Event Camera）+ 3D Gaussian Splatting，在不知道相机姿态的情况下，增量式重建 3D 场景。**

输入只有事件流，不需要 RGB 图像，不需要深度传感器，不需要 IMU。

---

## 二、核心洞察：事件相机 vs RGB 相机

这是理解整篇论文的关键：

```
RGB 相机:   测量绝对值 —— 像素 (x,y) 在时刻 t 的亮度 I(x,y,t)
事件相机:   测量变化值 —— 像素 (x,y) 在 [t1,t2] 期间亮度对数的变化量
            log(I_t2) - log(I_t1) = p × C  (p=±1 是极性, C 是阈值)
```

**这意味着什么？**

| | RGB SLAM | 事件 SLAM (本文) |
|---|---|---|
| 监督信号 | "这个像素应该是什么颜色" | "这两个时刻之间的亮度变化了多少" |
| 渲染 | 渲染 1 张图 | 渲染 **2 张图**，算 log-差 |
| Loss | |rendered - gt_photo|² | |lin_log(I₂) - lin_log(I₁) - gt_events|² |
| 本质 | 光度一致性 | **事件生成模型 (EGM) 一致性** |

这就是为什么要加 `lin_log` 函数——模拟事件相机的对数响应，同时避免 log(0)：

```python
# main.py:88
def lin_log(x):
    # x < 20: 线性 (避免log(0)爆炸)
    # x > 20: 对数 (匹配事件相机的对数响应)
    return torch.where(x > 20, torch.log(x), x/20 + np.log(20) - 1)
```

**合成事件 = lin_log(渲染图_t2) - lin_log(渲染图_t1)**，然后和真实事件累积图做 MSE Loss。

---

## 三、Pipeline 三阶段

训练正在跑，你应该看到这些阶段：

### 阶段 1：深度估计初始化 (4500 iter, ~8min)  ✅ 已完成

```
随机 100K Gaussians → 渲染事件图 → Marigold 估深度 → 用深度图反投影到 3D → 创建 SfM 风格的 Gaussian 初始化 → 联合优化 Gaussians + 姿态
```

**关键设计**：用事件 Loss（不是 RGB Loss）优化，因为是 event-only 系统。Marigold 只需要一张从事件隐式重建的灰度图。

### 阶段 2：SfM 初始化 (~500 iter) ✅ 已完成

```
用阶段1的深度图 + 事件约束 → 精化 Gaussian 位置 → 184K Gaussians
```

### 阶段 3：增量 Tracking + BA 🔄 进行中 (~5-6 小时)

```
═══════════════════════════════════════════
  Tracking (逐帧)          BA (滑动窗口)
  ─────────────────        ─────────────────
  固定 Gaussian              联合优化
  只优化新帧姿态              Gaussian + 多帧姿态
  
  每 2 次 Tracking → 1 次 BA
  每 N 次 BA → 1 次全局 BA
  
  ┌─────┬─────┬─────┬─────┬─────┐
  │ f1  │ f2  │ f3  │ ... │ fn  │  滑动窗口 = 20 帧
  └─────┴─────┴─────┴─────┴─────┘
         ← BA 窗口 →
         联合优化这个窗口内的所有姿态 + Gaussians
═══════════════════════════════════════════
```

轨迹用 **控制节点 + 样条插值** 表示在 SE(3) 李代数空间：
- 每个控制节点是 6D 向量 (3 旋转 + 3 平移)
- 任意时刻的姿态通过线性/Cubic B-spline 插值得到
- 这样做的好处：轨迹平滑、参数量少、不容易发散

---

## 四、核心创新点 (优势)

### 1. Pose-Free

**不需要任何初始姿态**。传统 SLAM 需要良好的初始姿态估计（容易漂移），本文完全从随机初始化开始，用事件 Loss 联合优化姿态 + 场景。这意味着：
- 没有 IMU 也能跑
- 高速运动不丢跟踪
- 极端光照 RGB 相机失效时也能工作

### 2. 事件生成模型 (EGM) 作为监督

不是简单的 "渲染图 vs 真值图" 比较，而是在 **事件空间** 做监督：

```
合成事件 = lin_log(渲染(I_t2)) - lin_log(渲染(I_t1))
Loss = MSE(合成事件, 真实事件累积图) + SSIM + ...
```

这有深刻含义：事件相机天然编码了**时域差分信息**，而 3DGS 可以高效渲染任意视角的图 → 两者天然匹配。

### 3. 增量式 Gaussian 生长

不是一次性初始化全部 Gaussian，而是在 Tracking/BA 过程中动态增补：
- 渲染事件图 → 发现 "事件密集但 alpha 低的区域" → 在深度面上撒新的 Gaussian
- 老的 Gaussian 被 prune（透明/太大/梯度小）

这也解释了为什么 Gaussian 数从 100K → 184K → 87K 变化。

### 4. 单通道 Grayscale + Lin-Log 色彩空间

事件相机本质上只感知亮度变化（不是颜色），所以渲染也是灰度图。Lin-log 映射是关键技巧——低亮度区线性（足够梯度）、高亮度区对数（匹配事件相机物理）。

### 5. 事件 Mask 机制

不是所有像素都有事件。用三种 Mask：
- **Event mask**: 只算有事件的像素
- **Visibility mask**: 只在 start 和 end 姿态都能看到的像素
- **Boundary mask**: 排除图像边缘（畸变大）

这确保 Loss 只在有效区域计算，避免噪声干扰。

---

## 五、局限性 (劣势)

### 1. 需要 Marigold 深度先验

**没有深度传感器，依赖单目深度估计网络。** Marigold 是 Stable Diffusion 模型（~5GB），推理一张图要几秒。深度不准 → Gaussian 初始化烂 → 后续全崩。

代码里的体现：`depth_estimation_init` 有三个选项：
- `0`：不用深度（基本跑不起来）
- `1`：运行时估计深度（我们用的）
- `2`：加载预计算深度（需要额外文件，我们没拿到）

### 2. 计算量大，不能实时

我们的 RTX 4090 跑一个 office0 场景（50 帧，998 张图）：

| 阶段 | 时间 |
|------|------|
| 深度初始化 | ~10 min |
| SfM 初始化 | ~3 min |
| Tracking + BA | ~5-6 hour |

**5-6 小时处理 50 帧**，离实时 SLAM 还差很远。主要是：
- Marigold 推理慢
- 3DGS 的 tile-based 光栅化不是为事件优化设计的
- 每次迭代都要渲染**两张图**（start + end），计算量翻倍
- 滑动窗口 BA 每次都重新渲染整个事件窗口

### 3. 场景规模受限

Bounding box = 3m。适合桌面/房间尺度物体，不能做大场景（户外、建筑）。

### 4. 只处理灰度信息

事件相机不感知颜色 → 重建出来的是灰度场景。虽然有 SH 系数，但 color_channels=1。论文说的是 "event-based"，实际上损失了颜色信息。

### 5. 事件数据预处理复杂

- 需要事件相机标定
- TUM-VIE 数据需要 fisheye 畸变校正
- 事件累积成图需要调阈值 (threshold=0.1/0.2)、窗口大小 (N_window=300K-1M events)
- 这些超参对不同场景/相机可能需要重新调

### 6. 依赖 GT 姿态做评估（但不用于训练）

代码里有 `traj.txt` 和 `poses_ts.txt`——仅用于：
- 评估指标计算（ATE/RPE）
- 数据集切分（确定哪些帧用于训练）
- **不参与 Loss 计算**

但如果没有 GT 姿态，你无法知道结果好不好。实际部署时这是个问题。

### 7. 对初始化敏感

`bounding_size` 被注释为 "very sensitive"——设大了 Gaussians 散太开优化慢，设小了场景放不下。目前需要人工设定。

---

## 六、与相关工作的对比

| 方法 | 传感器 | 是否需要初始姿态 | 场景表示 | 速度 |
|------|--------|:----------------:|----------|:----:|
| NICE-SLAM | RGB-D | 需要 | Neural Implicit | 慢 |
| CoSLAM | RGB-D | 需要 | Neural Implicit | 中 |
| 3DGS-SLAM | RGB-D | 需要 | 3D Gaussians | 较快 |
| **IncEventGS** | **Event only** | **不需要** | **3D Gaussians** | **慢** |

IncEventGS 的独特价值：**只用事件相机**（高速、高动态范围、低功耗），不需要深度传感器，不需要 RGB，不需要初始姿态。

---

## 七、一个具体例子：训练过程中发生了什么

从我们的训练日志：

```
iter 0:     loss=0.0537  event=0.0201  ssim=0.0335   ← 随机初始化，完全不对
iter 100:   loss=0.0457  event=0.0195  ssim=0.0262   ← 开始收敛
iter 500:   loss=0.0209  event=0.0050  ssim=0.0160   ← 大结构出现
iter 1500:  loss=0.0151  event=0.0024  ssim=0.0127   ← SfM 精化中
iter 3000:  loss=0.0118  event=0.0013  ssim=0.0104   ← 趋于稳定
iter 4500:  loss=0.0102  event=0.0011  ssim=0.0092   ← 初始化完成
```

SSIM loss 主导 → 说明事件 Loss 收敛快（差分约束强），SSIM 收敛慢（纹理细节需要更多迭代）。

初始化图片 `iter_0_vis.jpg → iter_4500_vis.jpg` 应该能看到从噪声 → 模糊轮廓 → 清晰结构的过程。

---

## 八、代码里值得注意的细节

1. **`main.py:88` lin_log**：阈值 20 是硬编码的，可能对不同光照需要调
2. **`main.py:749` post_process_event_image**：num-ba JIT 编译，首次调用慢但后续快
3. **`gsplat/rendering.py:202`**：`sh_degree = 0 # TODO: fix` — SH 颜色处理有 hack，可能影响渲染质量
4. **`main.py:2770` create_densify_points**：增量 Gaussian 生长的核心，检查 alpha 图找空洞
5. **`datasets/dataset.py:133` get_event_chunk**：事件按时间戳切片，关键是区间选择
6. **`spline/cubicSpline.py`**：SE(3) B-spline 的完整实现，200+ 行数学

---

## 九、总结

**IncEventGS 的独特贡献**：证明了事件相机 + 3DGS 可以做 pose-free SLAM。核心是把 EGM (事件生成模型) 变成可微分约束，在事件空间联合优化场景和姿态。

**最适合的场景**：
- 高速运动（RGB 相机会模糊，事件相机不会）
- 极端光照（RGB 相机会过曝/欠曝，事件相机动态范围 120dB+）
- 低功耗场景（事件相机功耗远低于 RGB 相机）

**当前最大的限制**：
- 太慢（离实时很远）
- 依赖单目深度估计
- 缺少颜色信息
- 超参数敏感
